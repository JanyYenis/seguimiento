<div class="text-gray-900 gap-1 pt-2">
    <!--begin::Heading-->
    <a href="#" class="text-gray-900">
        <span class="fw-bold {{ $isRead ? 'text-gray-400' : 'text-dark' }}">{{ $asunto ?? '' }}</span>
    </a>
    <!--end::Heading-->
</div>

{{-- @foreach ($etiquetas as $item)
    <div class="badge badge-light-primary">{{ $itrem }}</div>
@endforeach --}}
